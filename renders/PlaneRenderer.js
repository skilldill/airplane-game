import { BaseRenderer } from "./BaseRenderer";

export class PlaneRenderer extends BaseRenderer {
    constructor(ctx, template, colorsMap) {
        super(ctx, undefined, undefined, template, colorsMap);
    }
}